<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lalaland</title>


    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Lalaland</title>

    <!-- Bootstrap -->
    <link href="~/Content/bootstrap.min.css" rel="stylesheet">
    <!-- for fontawesome icon css file -->
    <link href="~/Content/font-awesome.min.css" rel="stylesheet">
    <!-- superslides css -->
    <link rel="stylesheet" href="~/Content/superslides.css">
    <!-- for content animate css file -->
    <link rel="stylesheet" href="~/Content/animate.css">
    <link rel="stylesheet" href="~/Content/bootstrap.css"/>
    <!-- slick slider css file -->
    <link href="~/Content/slick.css" rel="stylesheet">
    <link href="~/Content/jquery-ui.css" rel="stylesheet">
    <link href="~/Content/jquery-ui.min.css" rel="stylesheet">

    <!-- website theme color file -->
    <link id="switcher" href="~/Content/theme/cyan-theme.css" rel="stylesheet">
    <!-- main site css file -->
    <link href="~/style.css" rel="stylesheet">
    <!-- google fonts  -->
    <link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Raleway' rel='stylesheet' type='text/css'>
    <!-- Favicon -->
    <link rel="shortcut icon" href="~/img/ikona.ico" type="image/x-icon">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
    <!-- End footer -->
    <!-- jQuery Library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="~/Scripts/jquery-ui.js"></script>
    <script src="~/Scripts/jquery-ui.min.js"></script>
    <!-- For content animatin  -->
    <script src="~/Scripts/wow.min.js"></script>
    <!-- bootstrap js file -->
    <!-- superslides slider -->
    <script src="~/Scripts/jquery.easing.1.3.js"></script>
    <script src="~/Scripts/jquery.animate-enhanced.min.js"></script>
    <script src="~/Scripts/jquery.superslides.min.js" type="text/javascript" charset="utf-8"></script>
    <!-- slick slider js file -->
    <script src="~/Scripts/slick.min.js"></script>
    <!-- Google map -->
    <script src="https://maps.googleapis.com/maps/api/js"></script>
    <script src="~/Scripts/jquery.ui.map.js"></script>

    <!-- custom js file include -->
    <script src="~/Scripts/custom.js"></script>

    <script>
        $(".collapse.in").show().delay(5000).queue(function (n) {
            $(this).hide();
        });
    </script>

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")


    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</head>
<body>
    <!-- =========================
      //////////////This Theme Design and Developed //////////////////////
      //////////// by www.wpfreeware.com======================-->
    <!-- Preloader -->
    <div id="preloader">
        <div id="status">&nbsp;</div>
    </div>

    <!-- End Preloader -->
    <a class="scrollToTop" href="#"><i class="fa fa-angle-up"></i></a>

    <!-- start navbar -->
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <button type="button" id="maliNavbar" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a href="@Url.Action("Index", "Home")" class="navbar-brand">
                    <span>Lalaland</span>
                </a>  <!-- <a class="navbar-brand" href="index.html"><img src="img/logo.png" alt="logo"></a> -->
            </div>
          
            <div id="navbar" class="navbar-collapse collapse navbar_area " style="margin-left: 10px;">
                <ul class="nav navbar-nav navbar-right custom_nav" style="background-color:ghostwhite;">
                    <li>
                        <a href="@Url.Action("Index", "Home")" >
                            Home
                        </a>
                    </li>

                    <li>
                        <a href="@Url.Action("Index", "JobOffers")">
                            Job offers
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("About", "Home")">
                            Help
                        </a>
                    </li>

@if (User.Identity.IsAuthenticated)
{
                    <li>
                        <a href="@Url.Action("Index", "News")">
                            Local news
                        </a>
                    </li>
}
                   
                   

@if (User.Identity.IsAuthenticated)
{
                    <li>
                        <a href="@Url.Action("ListOfUsers", "Home")">
                            Followers
                        </a>
                    </li>
}

                    @if (User.Identity.IsAuthenticated)
                    {
                        if (ViewData["UserType"].Equals("Unemployed"))
                        {
                        <li class="dropdown" >
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Account <span class="caret"></span></a>
                            <ul class="dropdown-menu" role="menu">

                                <li>
                                    <a href="@Url.Action("EditUser", "Home")">
                                        Edit your account
                                    </a>
                                </li>
                                <li>
                                    <a href="@Url.Action("ImageFileUpload", "Home")">
                                        Change your profile image
                                    </a>
                                </li>
                                <li>
                                    <a href="@Url.Action("FileUpload", "Home")">
                                        Upload your CV
                                    </a>
                                </li>
                                <li align="center">
                                    @if (User.Identity.IsAuthenticated)
                                    {

                                        string imgSrc = null;

                                        if (ViewData["Content"] != null)
                                        {
                                            var base64 = Convert.ToBase64String((byte[])ViewData["Content"]);
                                            imgSrc = String.Format("data:image/gif;base64,{0}", base64);
                                            <img src="@imgSrc" style="max-width:120px; max-height:120px; border-radius: 25px;" />
                                            <a>@ViewData["FirstNameTry"] @ViewData["LastName"]</a>
                                        }
                                        else
                                        {
                                            <img src="~/img/nopic.png" style="max-width:120px; max-height:120px;" />
                                        }
                                    }
                                    else
                                    {
                                        <img src="~/img/nopic.png" style="max-width:120px; max-height:120px;" />
                                    }
                                </li>
                                <li>
                                    @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                                    { @Html.AntiForgeryToken()
                                        <button class="btn btn-danger" style="width:100%;" onclick="javascript:document.getElementById('logoutForm').submit()">Log off</button>
                                    }
                                </li>

                            </ul>
                        </li>
                        }
                        else if (ViewData["UserType"].Equals("Employer"))

                        {
                            <li class="dropdown" >
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Account <span class="caret"></span></a>
                                <ul class="dropdown-menu" role="menu">

                                    <li>
                                        <a href="@Url.Action("EditUser", "Home")">
                                            Edit your Company data
                                        </a>
                                    </li>
                                    <li>
                                        <a href="@Url.Action("ImageFileUpload", "Home")">
                                            Change your logo
                                        </a>
                                    </li>
                                    <li align="center">
                                        @if (User.Identity.IsAuthenticated)
                                        {

                                            string imgSrc = null;

                                            if (ViewData["Content"] != null)
                                            {
                                                var base64 = Convert.ToBase64String((byte[])ViewData["Content"]);
                                                imgSrc = String.Format("data:image/gif;base64,{0}", base64);
                                                <img src="@imgSrc" style="max-width:120px; max-height:120px; border-radius: 25px;" />
                                                <a>@ViewData["CompanyName"]</a>
                                            }
                                            else
                                            {
                                                <img src="~/img/nopic.png" style="max-width:120px; max-height:120px;" />
                                            }
                                        }
                                        else
                                        {
                                            <img src="~/img/nopic.png" style="max-width:120px; max-height:120px;" />
                                        }
                                    </li>
                                    <li>
                                        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                                        { @Html.AntiForgeryToken()
                                            <button class="btn btn-danger" style="width:100%;" onclick="javascript:document.getElementById('logoutForm').submit()">Log off</button>
                                        }
                                    </li>

                                </ul>
                            </li>
                        }
                    }
                    else
                    {
                        <li>
                            <a href="@Url.Action("Login", "Account")">
                                Sign in
                            </a>
                        </li>
                    }
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </nav>


    <div class="container body-content" align="center" >
        @RenderBody()

    </div>

</body>
</html>
