@model codeApp.Models.ArticlesMore

@{
    ViewBag.Title = "Get";
}

<h2>Get</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="col-md-6 col-md-offset-3">
    <ul>
        @foreach (var book in Model.artikli)
        {
            <li>@book.name</li>}
    </ul>
</div>
